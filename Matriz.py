def criar_matriz(num_linha, num_colunas,valor):
    matriz=[]
    for i in range(num_linha):
        linha=[]
        for j in range(num_colunas):
            linha.append(valor)
        matriz.append(linha\n)
    return matriz
